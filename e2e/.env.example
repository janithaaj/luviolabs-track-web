# Copy to e2e/.env (never commit e2e/.env).
# Values must match the admin created by the API seed (ADMIN_EMAIL / ADMIN_PASSWORD).

# Nest API must already be listening on http://localhost:4000
# PLAYWRIGHT_BASE_URL=http://localhost:3001

PLAYWRIGHT_ADMIN_EMAIL=
PLAYWRIGHT_ADMIN_PASSWORD=
