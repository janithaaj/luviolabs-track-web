# Copy to e2e/.env if you need non-seed credentials (optional).
# Defaults match the built-in admin seed — no file required for local smoke.

# Nest API must already be listening on http://localhost:4000
# PLAYWRIGHT_BASE_URL=http://localhost:3001

PLAYWRIGHT_ADMIN_EMAIL=admin@luvio.com
PLAYWRIGHT_ADMIN_PASSWORD=admin123
